#pragma once
#include "stdlib.h"

// Sets the allocator to use for the malloc family of functions
// as well as for new/delete when using C++.
class allocator;
void set_global_allocator(allocator *allocator);
